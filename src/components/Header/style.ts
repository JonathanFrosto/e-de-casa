import styled from "styled-components";

export const Header = styled.header`
  margin-bottom: 6rem;

  display: flex;
  justify-content: space-between;
  align-items: center;

  h1 {
    font-size: 2.5rem;
  }

  nav {
    cursor: pointer;
    display: flex;
    align-items: center;
  }
`